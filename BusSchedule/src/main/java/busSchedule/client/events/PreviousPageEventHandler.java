package busSchedule.client.events;

import com.google.gwt.event.shared.EventHandler;

public interface PreviousPageEventHandler extends EventHandler {
    void onPreviousPage(PreviousPageEvent previousPageEvent);
}